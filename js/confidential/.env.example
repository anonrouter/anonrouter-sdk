# Copy this file to `.env` (same directory) and fill in your values.
# `.env` is git-ignored and auto-loaded by the example scripts.

ANONROUTER_API_KEY=ar_your-inference-scoped-key

# Optional overrides (defaults shown):
# ANONROUTER_BASE_URL=https://api.anonrouter.ai
# TEE_PROVIDER=tinfoil
# TEE_MODEL=openai/gpt-oss-120b
# E2EE_PROVIDER=near-ai
# E2EE_MODEL=openai/gpt-oss-120b
# MAX_TOKENS=32
